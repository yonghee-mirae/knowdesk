# D-2 검색 결과 랭킹 규칙 — 옵션 비교

> `KnowDesk_추가검토사항.md` Part D의 D-2(검색 결과 랭킹 규칙) 결정을 위한 옵션 비교 자료다. 작성일 2026-08-20.
>
> D-2 원래 항목 중 "파일명 일치와 본문 일치의 가중치 비율"은 해당 없음으로 정리됐다 — `filename_fts`와 `content_fts`가 이미 테이블 단위로 완전 분리되어 있어 두 신호를 섞을 여지가 없기 때문이다(자세한 경위는 `KnowDesk_추가검토사항.md`의 ~~C-1~~ 정정 각주 참조). 아래는 남은 4개 하위 결정에 대한 옵션 비교다.

---

## 1. `content_fts` 컬럼 가중치 (body vs morph)

| 구분 | A. body 우세 | B. 동률 | C. morph 우세 | D. 설정 노출 |
|---|---|---|---|---|
| 예시 값 | body 1.0 / morph 0.3 | body 1.0 / morph 1.0 | body 0.3 / morph 1.0 | 사용자가 슬라이더로 조정 |
| 정확 일치 우선순위 | 높음 | 중간 | 낮음 | 가변 |
| 재현율 (`발행절차`→`발행` 매칭) | 낮음 | 중간 | 높음 | 가변 |
| 결과 예측 가능성 | 높음 | 중간 | 낮음 (형태소 매칭이 앞자리 차지 가능) | 낮음 (설정마다 다름) |
| 구현 난이도 | 낮음 (상수) | 낮음 | 낮음 | 중간 (설정 UI+검증 필요) |
| MVP 적합성 | 적합 | 적합 | 적합 | Phase 3급, MVP엔 과함 |

**추천**: A(body 우세) — 증권사 문서 검색은 정확한 용어·문구 일치가 중요하고, morph는 "덜 정확해도 놓치지 않게" 하는 보조 신호이므로 주 신호는 body가 맞음.

---

## 2. 수정일 최신성 가중치

| 구분 | A. 미적용 | B. 완만한 decay | C. 강한 decay | D. 랭킹 미반영, 필터/정렬로만 분리 |
|---|---|---|---|---|
| 동작 | 순수 텍스트 관련도만 | 최근 문서에 소폭 가점 | 최근 문서가 사실상 항상 상위 | `modified>` 필터 + "최신순" 정렬 옵션 별도 제공 |
| 오래된 규정집 검색 저해 위험 | 없음 | 낮음 | 높음 (구버전 규정집이 뒤로 밀림) | 없음 |
| "지난주 문서가 안 보인다" 불만 위험 | 있음 | 낮음 | 없음 | 없음 (사용자가 직접 정렬) |
| 구현 난이도 | 낮음 | 중간 (decay 함수 튜닝 필요) | 중간 | 낮음 (이미 `modified` 필터 존재) |

**추천**: D — 이미 `modified>`/`modified<` 필터가 있으므로(`05_Search_Language_v1.md`), 랭킹에 억지로 섞기보다 정렬 옵션(관련도순/최신순)으로 분리하는 게 규정집처럼 "오래됐지만 정확한" 문서를 죽이지 않음. 향후 사용자 피드백 보고 A→B 전환 여지 남김.

---

## 3. 다중 키워드 처리 (AND/OR/근접도)

| 구분 | A. 기본 AND | B. 기본 OR + bm25 정렬 | C. AND + Proximity 가점 |
|---|---|---|---|
| 동작 | 모든 키워드 포함 문서만 | 하나라도 포함, 관련도로 정렬 | 모두 포함 + 키워드 간 거리 가까울수록 가점 (`NEAR()`) |
| FTS5 네이티브 지원 | ✅ (공백 구분 시 기본 AND) | 쿼리를 OR로 변환 필요 | ✅ (`NEAR(a b, N)`) |
| 사용자 기대 부합 | 높음 (구글식 암묵 AND에 익숙) | 낮음 (결과 너무 많아짐) | 높음 |
| 구현 난이도 | 낮음 (이미 기본값) | 낮음 | 중간 (근접도 파라미터 튜닝) |
| 0건 위험 | 있음 (키워드 늘수록 0건 잦아짐) | 없음 | 있음 |

**추천**: A로 시작(이미 FTS5 기본 동작, 추가 구현 불필요) → 0건 시 동작(4번 항목)에서 완화 재검색으로 보완. C(근접도)는 Phase B 이후 품질 개선 항목으로 미루는 게 합리적.

---

## 4. Meta Index 문서 표시 방식

| 구분 | A. 통합 목록 | B. 분리 섹션 | C. 기본 숨김 (필터로만 노출) |
|---|---|---|---|
| 동작 | Full/Meta 한 리스트, 배지(`본문 미색인`)만 구분 | Full 결과 먼저, Meta는 하단 별도 섹션 | 기본 검색엔 안 뜨고 `tier:meta` 검색해야 노출 |
| 사용자가 "이 문서 존재는 안다" 인지 | 높음 | 높음 | 낮음 (있는지도 모름) |
| 리스트 오염(본문 없는 결과가 상위 섞임) 위험 | 있음 (파일명만으로 관련도 계산되어 순위가 들쭉날쭉) | 낮음 | 없음 |
| 구현 난이도 | 낮음 | 중간 (두 쿼리 결과 병합 UI) | 낮음 |

**추천**: B(분리 섹션) — `KnowDesk_추가검토사항.md` Part B-3에 이미 "`본문 미색인(보안 문서)` 배지" 표시가 명시돼 있어 완전 숨김(C)은 기존 정책과 어긋남. A는 관련도 계산 기준이 다른 두 결과를 한 순위표에 섞어 순서가 부자연스러워짐.

---

## 5. 검색 결과 0건 시 동작

| 구분 | A. 메시지만 | B. 오타 교정 제안 | C. 조건 완화 자동 재검색 (AND→OR) | D. B+C 조합 |
|---|---|---|---|---|
| 동작 | "결과 없음"만 표시 | fuzzy 유사어 제안 | 자동으로 조건 완화해 재검색 후 "완화된 결과" 표시 | 상황별 병행 |
| 구현 난이도 | 낮음 | 높음 (편집거리/유사도 알고리즘 필요) | 낮음 (쿼리 재조합만) | 높음 |
| MVP 적합성 | 적합 | Phase 2급, MVP엔 과함 | 적합 | 과함 |
| 사용자 체감 개선 | 낮음 | 높음 | 중간 | 높음 |

**추천**: C(조건 완화 자동 재검색) — AND로 시작(3번 항목)했을 때 0건이 잦을 것으로 예상되므로, 3번 결정과 짝을 이뤄 필요성이 크다. 오타 교정(B)은 별도 알고리즘이 필요해 MVP 범위 밖으로 미루는 게 맞음.

---

## 종합 추천안

| 항목 | 추천 |
|---|---|
| 1. body/morph 가중치 | A. body 우세 |
| 2. 최신성 가중치 | D. 랭킹 미반영, 정렬 옵션으로 분리 |
| 3. 다중 키워드 처리 | A. 기본 AND (근접도는 후순위) |
| 4. Meta 표시 방식 | B. 분리 섹션 |
| 5. 0건 시 동작 | C. 조건 완화 자동 재검색 |

이 추천안 확정 시 `01_KnowDesk_PRD.md` / `04_Data_Model.md` / `05_Search_Language_v1.md`에 반영이 필요하다.
